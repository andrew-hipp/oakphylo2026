All six files are drafted. Here's what was created:

notes from Claude after writing README files, 2026-05-06 9:20 central time

Top-level

CLAUDE.md — project overview, repository layout, data flow across the four sub-pipelines, conventions
Expanded existing READMEs

RADinitio/README.md — pipeline diagram, full parameter table, software deps, downstream connection to topologies/data/simulated_RAD/
genome_snps_and_refs/README.md — step-by-step table (0 → 9a/9b), a/b convention (Qalba/Qvar), portability caveat, key calling/filtering choices
New READMEs

fbranch_interp/README.md — inputs, what fb.R does, outputs (TABLE2 + FIG5), Dsuite citation
genome_snps_and_refs/9a_fbranch_speciesLevel/README.md — explains Fixlabels_*.sh, rename_tree.py, All_samples.txt, .rr tree files, pipeline diagram
genome_snps_and_refs/9b_fbranch_sectionLevel/README.md — same files for the section-level run, plus a note on what differs from 9a
Each file ends with the standard "This README was written by Claude Code (claude.ai/code) with Andrew Hipp's permission — 2026-05-06." line, matching the existing convention from topologies/README.md etc.

A few claims I made that you should sanity-check before merging:

The Q. alba / Q. variabilis interpretation of the a / b step suffixes in genome_snps_and_refs/.
That 1_pretreat_SRR18459034.sh was a one-off targeting that single accession.
The enz=PstI, sdRAD, etc. parameter values in RADinitio (read directly from the script, but worth confirming they were intentional).
Whether you'd like the citations for RADinitio (Rivera-Colón et al. 2021) and Dsuite (Malinsky et al. 2021) — I added them; remove if you'd rather keep references only in the manuscript.